#include "stdafx.h"
#include "AStarRestarting.h"



/*void AStarRestarting::insert(Node * n)
{
	auto it = open->begin();
	for (; it != open->end() && (*it)->fw < n->fw; it++);
	open->insert(it, n);
}*/

